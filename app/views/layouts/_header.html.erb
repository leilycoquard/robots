<div class="container">
  <div class="d-flex justify-content-between">
    <div class="col-2">
      <%= image_tag("logo.png", class: "img-fluid")%>
    </div>
    <div class="col-4">
      <% if logged_in? %>
        <p><%= current_user.username %></p>
    <%end%>
    </div>
  </div>
</div>
