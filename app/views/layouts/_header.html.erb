<div class="container">

  <div class="d-flex justify-content-between">

    <div class="col-1">
      <%= image_tag("logo.png", class: "img-fluid")%>
    </div>

    <div class="col-4 d-flex justify-content-between">
      <% if logged_in? %>
        <p><%= current_user.username %></p>
        <%= button_to "Déconnexion", logout_path, method: "delete" %></li>
      <%end%>
    </div>

  </div>

</div>
